<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="madera.Views.ClientList"
             BackgroundImageSource="BackGroud_Portrait_2.jpg"
             Title="List_Client">
    <ContentPage.Content>
        <StackLayout>
            <ScrollView>
                <ListView HeightRequest ="800" ItemsSource="{Binding Clients}" ItemTapped="MainListView_ItemTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ImageCell Text="{Binding FullName}"
                              Detail="{Binding Ville}" ImageSource="client.png" TextColor ="white"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>